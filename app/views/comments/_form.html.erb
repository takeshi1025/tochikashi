<%= form_with(model: [tochi, comment], url: tochi_comments_path(@tochi) ) do |f| %>
    <%= f.text_area :content, class: "input-mysize" %>
    <%= f.submit "送信", class: "btn btn-outline-primary comment-submit float-right mt-4" %>
<% end %>
